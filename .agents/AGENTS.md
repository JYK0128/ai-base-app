# Agent Guidelines

공유 환경(Shared Environment) 기반 작업 수칙.

---

## 📋 Interaction Protocols

## 1. **한국어 우선 (Korean First)**

- 모든 답변, 문서, 주석 등 모든 출력물은 한국어 작성을 원칙으로 함.
- `SKILL.md`, `agents/openai.yaml`, 체크리스트, 사양서 등 에이전트가 생성·수정하는 메타데이터/문서 파일도 예외 없이 한국어를 기본으로 작성함. 단, 코드·명령어·경로·기술 식별자·환경 변수명은 원문 유지 가능.
- [필수] 모든 답변은 서술형을 배제하고, 반드시 명확하고 간결한 **개조식(Bullet points)** 형식을 사용하여 보고함.
- 오류 발생 시 즉시 모든 작업을 중단하고, 추가적인 독자적 조사나 수정 없이 원인 분석 결과와 해결 방안을 사용자에게 즉시 보고한 후 승인을 받아 진행함.

## 2. **서술 및 언어 스타일 (Writing Style & Tone)**

- **긍정형 언어 표준 (Positive Phrasing)**: 모든 스킬 가이드 문서, 소스코드 내의 주석(Comments), 커밋 메시지, PR 본문 및 모든 설명 문구(Descriptions) 작성 및 수정 시 "금지어(Negative phrasing)" 및 "A 대신 B" 형태의 부정/비교 문구를 일절 사용하지 않으며, 수행해야 하는 구체적인 동작과 지침을 명확히 명문화한 긍정형 제안(Positive Directives)만 작성함.
- **간결한 개조식 (Bullet Points)**: 답변과 문서 작성 시 서술형(줄글)을 배제하고 명확하고 간결한 개조식(Bullet points) 형식을 일관되게 유지함.
- **기술 중심의 명시성**: 모호하거나 직관적이지 않은 사설을 배제하고, 구체적인 코드 패턴 및 표준화된 아키텍처 규칙만 명시적으로 기술함.

## 3. **터미널 공유 (Terminal Sharing)**

- 제공된 **`ProcessId`** 기반으로 활동 조율 및 실시간 상태 추적.
- 기존 서버 프로세스 임의 종료 절대 금지.
- 중복 터미널 생성을 지양하고 활성 세션 내에서 작업함.

## 4. **브라우저 공유 (Browser Sharing)**

- 제공된 활성 세션(**`PageId`**) 및 공유 페이지를 반드시 활용함.
- 독립 브라우저 실행을 금지하고 사용자 화면과 컨텍스트를 동기화함.
- UI 검증은 항상 공유된 브라우저 페이지에서 수행함.

## 5. **상태 지속성 (State Persistence)**

- 대화 간 **`ProcessId`**, **`PageId`**를 추적하여 작업 컨텍스트를 완벽히 유지함.

## 6. **작업 범위 (Scope of Work)**

- 문서 검토 후, 백엔드(API) 및 프론트엔드(UI) 작업을 통합하여 유기적으로 수행함.
- 엔티티 설계부터 브라우저 검증까지 포함된 완결된 결과물을 전달함.
- 구현 전후 내용에 맞춰 관련 기술 문서(사양서, 아키텍처 등)를 반드시 최신화함.

## 7. **적응형 문제 해결 (3-Strike Rule)**

- 동일 동작 실패 시 3회를 초과하여 반복하지 않음.
- 실패 시 즉시 모든 도구 호출을 중단하고 로그를 분석하여 사용자에게 상황을 보고함. (추가 도구 호출 금지)
- 분석된 대안(스크립트 방식 등)을 사용자에게 보고하여 확인받은 후 작업을 재개함.

## 8. **문서 및 스킬 기반 작업 (Documentation & Skills First)**

- 모든 기술적 의사결정 및 구현 시 `.docs/` 디렉토리 내의 사양서, 아키텍처, 개발 표준을 상시 참조함.
- 특히 `05_references` 폴더의 인터페이스/백엔드/프론트엔드 표준을 최우선으로 준수하여 프로젝트의 일관성을 유지함.
- **도메인별 스킬 가이드 준수**: 특정 도메인의 작업(인프라, 레이아웃, 모노레포 패키지 추가, CQRS, ORM 엔티티 등)을 수행할 때는 `.agents/skills/` 디렉토리에 정의된 각 스킬의 `SKILL.md`와 이에 연계된 순차 번호가 매겨진 참조 문서(`references/01_...`)를 선제적으로 조회하고 그 기준을 엄격히 준수함. (단, 시스템/외부 연동용 스킬인 `skill-creator`는 원본 형상을 유지해야 하므로 어떠한 수정이나 변경도 금지함.)

## 9. **파일 관리 및 임시 작업 (File Management & Temp Works)**

- 디버깅, 가동 테스트, **설치 도구(바이너리, 스크립트 등)**를 포함하여 작업 중 발생하는 모든 임시 파일은 반드시 루트의 **`.tmp/`** 디렉토리에서 관리함.
- 프로젝트 루트에 직접 임시 파일을 생성하는 것을 금지하며, 작업 종료 후에는 해당 파일을 정리하여 프로젝트 환경을 깨끗하게 유지함.
