import { Settings } from 'lucide-react';

export function SettingsTab() {
  return (
    <div className="animate-fade-in flex flex-col items-center justify-center py-20 text-slate-500">
      <Settings className="w-12 h-12 mb-4 opacity-20" />
      <p>기타 설정 화면 준비 중입니다.</p>
    </div>
  );
}
