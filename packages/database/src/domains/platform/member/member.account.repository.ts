import { CoreRepository } from '../../core/core.repository';
import type { MemberAccount } from './member.account.entity';

export class MemberAccountRepository extends CoreRepository<MemberAccount> {
}
