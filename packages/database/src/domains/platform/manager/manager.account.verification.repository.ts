import { CoreRepository } from '../../core/core.repository';
import type { ManagerAccountVerification } from './manager.account.verification.entity';

export class ManagerAccountVerificationRepository extends CoreRepository<ManagerAccountVerification> {
}
