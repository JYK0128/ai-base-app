apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mimir-sts
  namespace: monitoring
  labels:
    app.kubernetes.io/name: mimir
    app.kubernetes.io/part-of: ai-base-app
spec:
  serviceName: "mimir"
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mimir
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mimir
    spec:
      containers:
      - name: mimir
        image: public.ecr.aws/bitnami/grafana-mimir:latest
        imagePullPolicy: IfNotPresent
        workingDir: /tmp
        args:
        - "-config.file=/etc/mimir/mimir.yaml"
        - "-target=all"
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 40
          periodSeconds: 15
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        volumeMounts:
        - name: config
          mountPath: /etc/mimir
        - name: data
          mountPath: /data/mimir
        - name: alert-rules
          mountPath: /data/mimir/rules/anonymous
          readOnly: true
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: config
        configMap:
          name: mimir-cm
      - name: alert-rules
        configMap:
          name: monitoring-alerts-cm
          items:
          - key: monitoring-rules.yaml
            path: monitoring-rules.yaml
      - name: tmp
        emptyDir: {}
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 20Gi
