apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-service-gateway-to-platform-auth
  namespace: dev-web
spec:
  podSelector:
    matchLabels:
      app: service-gateway
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: dev-api
      podSelector:
        matchLabels:
          app: platform-auth-service
    ports:
    - protocol: TCP
      port: 3000
