apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-platform-auth-to-postgres
  namespace: dev-api
spec:
  podSelector:
    matchLabels:
      app: platform-auth-service
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: dev-infra
      podSelector:
        matchLabels:
          app: postgres
    ports:
    - protocol: TCP
      port: 5432
