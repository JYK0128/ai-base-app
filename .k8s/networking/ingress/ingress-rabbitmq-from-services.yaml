apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ingress-rabbitmq-from-services
  namespace: dev-infra
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: rabbitmq
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: dev-service
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ambassador
    ports:
    - protocol: TCP
      port: 5672

