apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: dev-infra
  labels:
    app: kafka
spec:
  selector:
    app: kafka
  ports:
  - name: broker
    port: 9092
    targetPort: kafka
  - name: controller
    port: 9093
    targetPort: controller
  type: ClusterIP
