apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: rabbitmq
  namespace: dev-infra
spec:
  replicas: 1
  secretBackend:
    externalSecret:
      name: rabbitmq-secret
  image: rabbitmq:4-management
  persistence:
    storage: "0"
